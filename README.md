# 20-large-many-files-fastapi-root

Fixture: `20-large-many-files-fastapi-root`

Expected primary classification: `fastapi`

Expected labels: python, fastapi

Why this fixture exists: Large repo where SBOM avoids needing tree scans.

Failure expected/informational: no

Notes: Validates that SBOM avoids needing tree scans for large repos
